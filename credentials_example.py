# add the credentials for sending alert emails and save as credentials.py
# make sure to keep it gitignored and NOT push it to a public repository!
sender_email = "foo@bar.com"
sender_email_pw = "YOURPASSWORD"
receiver_email = ["example@foo.com", "example2@foo.com",]